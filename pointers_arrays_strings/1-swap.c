#include "main.h"
/**
 * *swap_int returns the sign of a number
 * *@b: number to be checked
 * *@a: akshdfioa
 * *Return: -1, 1 or 0
 */
void swap_int(int *a, int *b)
{
	int p;
p = *a;
	*a = *b;
	*b = p;
}
