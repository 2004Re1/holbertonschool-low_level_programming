#include "main.h"
#include <string.h>
/**
 * *_strlen - returns the sign of a number
 * *@s: number to be checked
 * *Return: -1, 1 or 0
 */
int _strlen(char *s)
{
	int n;
	n = strlen(s);
	return (n);
}
